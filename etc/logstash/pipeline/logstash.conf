input {
    file {
        path => "/logs/*"
        start_position => "beginning"
        codec => "json"
    }
}

filter {
    date {
        match => ["Fecha" , "d/M/yyyy HH:mm:ss"]
    }
}

output {
  elasticsearch {
        index => "covid19"
        hosts => ["elasticsearch:9200"]
    }
}